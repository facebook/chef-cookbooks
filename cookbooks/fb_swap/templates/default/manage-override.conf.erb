[Unit]
BindsTo=manage-swap-<%= @type %>.service
After=manage-swap-<%= @type %>.service
PartOf=manage-swap-<%= @type %>.service

# T39598868 Disabled until workaround or bug fix
# [Service]
# # Stopping swap is pathologically slow on Linux today. The general default
# # for stopping units in systemd is 90s. Here we'll use 100s per GiB as a
# # heuristic on top of the default.
# TimeoutStopSec=<%= 90 + 100 * (node['fb_swap']['_calculated']["#{@type}_size_bytes"] / 2 ** 30) %>
